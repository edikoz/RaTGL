#pragma once

//Add "visible" and "compute" properties to elements

//Fix scrollbar in Properies (repaired a little)

//Add indicators of rotating and translating in the transform element

//Element.cpp std::string Properties::transform::getShader()
//ret = replaceString(ret, "RAY", "r" + std::to_string(ray - 1));
//or
//ret = replaceString(ret, "RAY", "r" + std::to_string(ray++));

//Move GLwindow ctor to protected

//Add virtual mat calc to elements